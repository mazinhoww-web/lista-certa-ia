// Thin re-export so consumers can `import { useAuth } from "@/hooks/useAuth"`
// alongside the rest of the hooks. Implementation lives in AuthContext.
export { useAuth } from "@/contexts/AuthContext";
