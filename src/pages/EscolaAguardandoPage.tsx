// LC-003 deprecated this stub. Any external link or stale tab still
// pointing here is bounced to the unified hub. Kept as a redirect (rather
// than fully removed) to avoid 404s. Will be deleted in a later cleanup.

import { Navigate } from "react-router-dom";

export default function EscolaAguardandoPage() {
  return <Navigate to="/minhas-escolas" replace />;
}
